# TODO - 交付待办清单 (MovieRec)

本文档基于 `TRACK_MovieRec.md` 的最终交付状态，精简列出项目后续待办的交接及收尾事项，用于辅助人工介入与二次开发。

## 环境及参数缺失项补充
- [ ] **环境变量保护 (`.env` 治理)**
  本部分代码中连接远端微服务的凭据（例如 DB, Redis, Kafka 访问信息），禁止保留在代码硬编码环节。接手人须检查应用是否引入全局统一环境凭据变量表。
- [ ] **模型快照挂载与路由**
  在 `inference/main.py` 内部验证装载的神经元网络快照后缀 `*.pt`/`*.pth` 模型路径是否准确，并且已完成在测试训练轮数的对齐检查。

## 剩余待解决及操作指引
1. **服务容器编排配置确认 (`docker-compose.yml`)**
   为方便后继接手人一键部署整体验证环节，应当于根目录完成 `docker-compose.yml` 的组合补全——串联应用系统服务、MySQL、Kafka 及挂载带有 GPU 能力分配的 `nvidia-runtime` 机器学习容器。
2. **端到端负载测试**
   依托 `JMeter` 或对应压测工具对 Spring Boot 控制器层面及前端防抖阀值进行极限边界性能定级（基准响应：模型请求链路需在 150ms 超时阈值内不被强行削峰或阻断降频为兜底模式）。
3. **性能基准评估日志观测**
   接手人工校验消费组（`datalake-archiver` 与 `realtime-profiler`）中消息日志的积压消费水平，确保系统运行没有内存耗尽异常及消费死信累积情况。
